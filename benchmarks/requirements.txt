# Dependencies for the benchmarks in this directory.
#
# The library itself (lazy_attn) needs almost nothing beyond vLLM -- see
# lazy_attn/pyproject.toml. Install the backend first with
# `bash scripts/install.sh`, which pins vLLM to a matching torch build; do not
# add torch/transformers/vllm here or pip will fight those pins.
#
# Equivalent to: pip install -e lazy_attn[bench,dev]

# Benchmark harness
datasets
fastapi
loguru
pandas
pydantic
requests
scipy
simple_parsing
uvicorn

# Accuracy scoring (LongBench / HotpotQA / ChatRAG metrics)
fuzzywuzzy
jieba
python-Levenshtein
rouge

# Test + lint
pytest
pytest-cov
pytest-rerunfailures
pytest-xdist
